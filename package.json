{
  "name": "game-caterpillar",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "tsc -p worker --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260914.1",
    "typescript": "~6.0.2",
    "wrangler": "^4.131.2"
  }
}
